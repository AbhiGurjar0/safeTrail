<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Emergency SOS</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-white font-sans text-gray-700 ">
  <div class="sticky top-0 z-50 h-[5vh]">
    <%- include('partials/navbar', { user: user }) %>
  </div>
  <div class="flex justify-center items-start p-10 min-h-[95vh] w-full bg-gray-100">
    <div class="max-w-lg w-full">
      <!-- Icon -->
      <div class="flex justify-center mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-red-500" fill="none" viewBox="0 0 24 24"
          stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M21 12l-9 9-9-9 9-9 9 9z" />
        </svg>
      </div>

      <!-- Heading -->
      <h1 class="text-2xl font-bold text-red-600 text-center mb-2">Emergency SOS</h1>
      <p class="text-center text-gray-600 mb-8">This will immediately alert emergency services to your location</p>

      <!-- Main container -->
      <div class="border border-gray-200 rounded-lg p-6 space-y-6">
        <!-- Section heading -->
        <div class="text-center">
          <h2 class="text-red-600 font-bold text-xl mb-1">Emergency Alert System</h2>
          <p class="text-gray-600 text-sm">Press the button below only in case of real emergency</p>
        </div>

        <!-- Info box -->
        <div class="bg-red-50 border border-red-100 rounded-md p-4 space-y-3 text-gray-800 text-sm">
          <p class="font-semibold text-red-600">When you press SOS:</p>
          <ul class="list-disc list-inside space-y-1">
            <li>Your exact location will be shared with authorities</li>
            <li>Your tourist ID and personal details will be sent</li>
            <li>Emergency contact will be automatically notified</li>
            <li>Nearest emergency services will be dispatched</li>
          </ul>
        </div>

        <!-- SOS Button -->
        <button
          class="w-full bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white font-semibold py-3 rounded-md flex items-center justify-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"
            stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M21 12l-9 9-9-9 9-9 9 9z" />
          </svg>
          SEND SOS ALERT
        </button>

        <p class="text-center text-gray-500 text-xs mt-2">This action cannot be undone. Only use in genuine emergencies.
        </p>

        <hr class="my-6 border-gray-200" />

        <!-- Emergency Contacts -->
        <div class="text-gray-800 text-sm space-y-3">
          <p class="font-semibold">Emergency Contacts:</p>
          <div class="flex justify-between">
            <p>Local Emergency Services:</p>
            <p>112</p>
          </div>
          <div class="flex justify-between">
            <p>Tourist Helpline:</p>
            <p>+33 123 456 789</p>
          </div>
          <div class="flex justify-between">
            <p>Your Emergency Contact:</p>
            <p>+1 555 123 4567</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>