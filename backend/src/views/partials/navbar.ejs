<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <header class="bg-white shadow-md py-4 px-6 md:px-12 flex items-center justify-between">
        <a href="#" class="flex items-center space-x-2">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-primary" viewBox="0 0 24 24"
                fill="currentColor">
                <path
                    d="M12 2L2 7v10l10 5 10-5V7L12 2zm0 2.3l6.5 3.25L12 10.8l-6.5-3.25L12 4.3zM4 9l8 4 8-4v6l-8 4-8-4V9z" />
            </svg>
            <span class="text-xl font-bold text-dark">TouristSafe</span>
        </a>
        <nav class="hidden md:flex items-center space-x-6">
            <a href="/" class="text-gray-600 hover:text-primary transition duration-300">Home</a>
            <a href="/emergencysos" class="text-gray-600 hover:text-primary transition duration-300">Emergency</a>
            <a href="/dashboard" class="text-gray-600 hover:text-primary transition duration-300">Dashboard</a>
        </nav>
        <div class="flex items-center space-x-4">
            <% if(user && user.length>0){ %>
                <a href="/login"
                    class="text-gray-600 hover:text-primary transition duration-300 hidden md:inline-block">Login</a>
                <a href="/register"
                    class="bg-primary hover:bg-secondary text-white font-semibold py-2 px-6 rounded-full shadow-md hover:shadow-lg transition duration-300">
                    Get Started
                </a>
                <% } else { %>
                    <a href="/profile" class="flex items-center space-x-2">
                        <img src="https://placehold.co/32x32/E5E7EB/4B5563?text=<%= user.name.substring(0,2).toUpperCase() %>"
                            alt="Profile" class="w-8 h-8 rounded-full border border-gray-300">
                        <span class="text-gray-700 font-semibold">
                            <%= user.name %>
                        </span>
                    </a>
                    <% } %>
        </div>
    </header>

</body>

</html>